from django import forms


class HelloForm(forms.Form):
    your_name = forms.CharField(
        label = 'NAME',
        max_langth=20,
        required=True,
        widget=forms.TextInput()
        )
